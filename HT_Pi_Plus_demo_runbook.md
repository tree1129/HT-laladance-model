# HT Pi Plus 啦啦操 Demo 现场操作手册

这份手册用于真机现场操作。原则是先确认停止，再测试移动，最后测试动作和编排。

## 1. 开始前安全检查

1. 清空机器人周围地面，旁边安排一人保护。
2. 确认机器人电量足够，身体结构无明显松动。
3. 确认你知道物理急停或断电方式。
4. 电脑和机器人连接到同一网络。
5. 第一次测试不要让机器人离人太远，不要直接跑完整编排。

## 2. 连接检查

默认配置里的机器人地址是：

```text
192.168.43.44
```

在电脑 PowerShell 里先检查网络：

```powershell
ping 192.168.43.44
```

如果 ping 不通，先处理网络，不要继续跑控制程序。

然后检查 SSH：

```powershell
ssh hightorque@192.168.43.44
```

能登录后，再确认机器人端 ROS 和官方控制程序是否已经按官方流程启动。

## 3. 启动网页遥控器

优先方案：机器人端服务。

在 Windows PowerShell / PyCharm 默认终端中，先部署：

```powershell
cd <你的项目目录>\robot_remote_web
.\deploy_robot_agent.ps1 192.168.43.44 hightorque
```

在机器人上运行：

```bash
cd /home/hightorque/robot_remote_web_agent/robot_agent
bash start_robot_agent.sh
```

然后电脑浏览器打开：

```text
http://192.168.43.44:8766
```

备用方案：电脑端 SSH 代理。

在电脑当前项目目录运行：

```powershell
cd <你的项目目录>\robot_remote_web
.\start_remote.ps1
```

然后浏览器打开：

```text
http://127.0.0.1:8765
```

## 4. 最小测试顺序

不要跳步。

1. 点击“急停”，确认页面日志显示停止指令已发送。
2. 点击“唤醒运动模式”。
3. 点击“低速前进”，只按住很短时间，松开后确认自动停止。
4. 按 `Space`，确认急停可用。
5. 按 `R`，测试安全原地踏步，再按 `Space` 停止。
6. 点击“双手欢呼”，确认上肢动作能单独播放。
7. 点击“安全啦啦操编排”，只在前面步骤都稳定后测试。

## 5. 键盘控制

- `W/A/S/D`：低速前后左右。
- `Q/E`：低速转向。
- `R`：安全原地踏步开关。
- `C`：双手欢呼。
- `Space`：急停。

## 6. 推荐 Demo 顺序

1. 急停。
2. 唤醒运动模式。
3. 安全原地踏步 3 秒。
4. 双手欢呼。
5. 低速前进 2-4 秒。
6. 双手欢呼。
7. 急停。

## 7. 降级方案

如果“边走边上肢动作”不稳，立刻降级：

```text
站立 -> 双手欢呼 -> 安全原地踏步 -> 急停
```

如果原地踏步也不稳，再降级：

```text
站立 -> 双手欢呼 -> 结束姿势
```

交付优先级是稳定和安全，不是动作复杂度。
